{
  "name": "as-ui-auth",
  "version": "0.0.1",
  "description": "Example client side ui components for authenticated systems workshop",
  "scripts": {
    "coverage": "istanbul cover --recursive jasmine-node test; istanbul check-coverage  --statements 100 --functions 94 --branches 86 --lines 100",
    "lint": "jshint components/*/**.js test/*/**.js",
    "postinstall": "bower install",
    "test": "jasmine-node test",
    "test:browser": "open SpecRunner.html"
  },
  "keywords": [
    "authentication",
    "oauth2",
    "jwt"
  ],
  "author": "cesine <cesine@yahoo.com>",
  "license": "MIT",
  "devDependencies": {
    "fetch-mock": "^4.6.0",
    "istanbul": "^0.4.4",
    "jasmine-node": "^1.14.5",
    "node-fetch": "^1.5.3"
  }
}
